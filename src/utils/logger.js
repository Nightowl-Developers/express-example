export const createHttpLogger = (
    url,
    level,
    format
) => {
    // create http based logger
};

/**
 * 
 * @param {object} payload - The object being logged.
 * 
 * payload: {
 *   status: HttpStatusCode,
 *   payload: object,
 *   errorMessage: string
 * }
 */
export const log = ({
    status,
    payload,
    errorMessage
}) => {
    // log something
};